# Frontend

This folder contains the Vite React frontend.

## Commands

- `npm run dev --workspace @fonthabesha/frontend`
- `npm run build --workspace @fonthabesha/frontend`

## Environment

Set `VITE_API_URL` in `.env` to the backend API base URL.
