// Legacy entry point — app is bootstrapped via main.tsx → RouterProvider.
// This file is kept to satisfy any tooling that expects a default App export.
export default function App() {
  return null;
}
